name: Release

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  precommit:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: msfailab_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: ecto://postgres:postgres@localhost/msfailab_test

    steps:
      - uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19.2"
          otp-version: "28.1.1"

      - name: Cache Elixir dependencies
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: mix-${{ hashFiles('mix.lock') }}
          restore-keys: mix-

      - name: Cache Node dependencies
        uses: actions/cache@v4
        with:
          path: assets/node_modules
          key: npm-${{ hashFiles('assets/package-lock.json') }}
          restore-keys: npm-

      - name: Setup
        run: mix setup

      - name: Run precommit
        run: mix precommit

  build-and-push:
    needs: precommit
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build and push msfailab-server (Elixir app)
      - name: Extract metadata for server
        id: meta-server
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-server
          tags: |
            type=raw,value=latest
            type=sha,prefix=

      - name: Build and push server
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta-server.outputs.tags }}
          labels: ${{ steps.meta-server.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ github.repository }}-server:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ github.repository }}-server:buildcache,mode=max

      # Build and push msfailab-msfconsole (Kali + Metasploit)
      - name: Extract metadata for msfconsole
        id: meta-msfconsole
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-msfconsole
          tags: |
            type=raw,value=latest
            type=sha,prefix=

      - name: Build and push msfconsole
        uses: docker/build-push-action@v6
        with:
          context: ./msfconsole
          push: true
          tags: ${{ steps.meta-msfconsole.outputs.tags }}
          labels: ${{ steps.meta-msfconsole.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ github.repository }}-msfconsole:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ github.repository }}-msfconsole:buildcache,mode=max

      # Publish docker-compose.yml as OCI artifact
      - name: Publish compose file
        run: docker compose -f docker-compose.release.yml publish -y ${{ env.REGISTRY }}/${{ github.repository }}:latest
